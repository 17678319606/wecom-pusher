# EdgeOne 部署所需环境变量（在 EdgeOne 控制台 → Pages 项目 → 环境变量 中设置，不要提交真实值）
# 这些变量由 Functions 的 context.env 读取，不依赖本文件。

# KV 命名空间绑定变量名（固定为 KV，必填）
KV=subjiqiren

# 后台管理令牌：用于 /api/ad 等管理接口鉴权（header x-admin-token 或 ?token=）
# 请使用强随机值，并在 EdgeOne 控制台设置，切勿提交到仓库。
ADMIN_TOKEN=change_me_to_a_strong_random_value
